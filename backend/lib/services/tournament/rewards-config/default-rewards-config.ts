// ==========================================
// Default rewards configuration — code only (no file).
// Single source: default-rewards-constants.ts. Used by Initialize and by admin UI.
// ==========================================

export type { DefaultRewardConfig } from './default-rewards-constants';
export { getSystemDefaultRewards } from './default-rewards-constants';
