# Game sound effects (optional)

The game uses **procedural/oscillator sounds** by default (see `sound-effects.js`). No WAV files are required for gameplay.

This folder is only used if you enable optional sample-based sounds (e.g. in a sound test UI). If you add WAVs here, they can be used as overrides; the config is in `audio-sample-config.js`.
