`# Sui Smart Contract Examples Environment Configuration
# Copy this file to .env.local and fill in your actual values

# Smart Contract Configuration
# Replace these with your deployed contract IDs from the examples
NEXT_PUBLIC_PACKAGE_ID=YOUR_PACKAGE_ID_HERE
NEXT_PUBLIC_SCORE_SYSTEM_ID=YOUR_SCORE_SYSTEM_OBJECT_ID_HERE
NEXT_PUBLIC_ADMIN_SYSTEM_ID=YOUR_ADMIN_SYSTEM_OBJECT_ID_HERE
NEXT_PUBLIC_GAME_PASS_SYSTEM_ID=YOUR_GAME_PASS_SYSTEM_OBJECT_ID_HERE
NEXT_PUBLIC_CLOCK_ID=0x6

# Network Configuration
# Choose your network: testnet, mainnet, or devnet
NEXT_PUBLIC_NETWORK_TYPE=testnet
NEXT_PUBLIC_SUI_RPC_URL=https://fullnode.testnet.sui.io:443

# Application Configuration
# Your application's base URL
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Instructions:
# 1. Deploy the smart contracts from contracts/examples/
# 2. Copy the Package ID and Object IDs from the deployment output
# 3. Replace the placeholder values above with your actual contract IDs
# 4. Update the network type and RPC URL if needed
# 5. Never commit .env.local to version control